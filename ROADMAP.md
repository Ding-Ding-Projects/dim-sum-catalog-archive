# Roadmap

## Archive maintenance

- [x] Preserve the generated catalog, image manifest, schema, and catalog parts in Git.
- [x] Keep image release-asset metadata auditable through `manifest/archive-manifest.json`.
- [x] Record repository state and closeout evidence in `HANDOFF.md`.
- [ ] Refresh release assets only when a separately scoped archive update is requested.

## Current state

The catalog archive is complete for the current tracked data. No implementation or release
work is pending in this maintenance pass. The unchecked item is intentionally retained as
future work and was not started here.
